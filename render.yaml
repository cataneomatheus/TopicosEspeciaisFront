services:
  - type: web
    name: topicos-especiais-front
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    branch: main
    envVars:
      - key: VITE_API_URL
        sync: false
